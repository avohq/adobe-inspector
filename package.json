{
    "name": "adobe-inspector-web",
    "version": "1.2.0",
    "description": "Send analytics tracking to Avo Inspector to observe and validate your tracking schema.",
    "dependencies": {},
    "engines": {
        "node": ">=v14.21.2"
    },
    "scripts": {
        "dev": "npx @adobe/reactor-sandbox",
        "build": "rm -rf node_modules && rm -rf .sandbox && npx @adobe/reactor-packager",
        "upload": "npx @adobe/reactor-uploader",
        "release": "npx @adobe/reactor-releaser"
    }
}
